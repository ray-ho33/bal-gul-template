# bal-gul agent guidance

## 설치 요청을 받았을 때

- 사용자가 저장소 URL과 함께 설치 또는 설정을 요청하면 설치 방식을 되묻지 않는다.
- `README.md`, `SETUP.md`, `SETUP_PROMPT.md`를 순서대로 읽고 이를 설치 절차의 기준으로 삼는다.
- 이미 끝난 단계를 확인한 뒤, 아직 완료되지 않은 첫 단계부터 한 단계씩 안내한다.
- 기본 안내 언어는 한국어로 하고, 사용자가 다른 언어를 요청하면 그 언어를 사용한다.
- 에이전트가 안전하게 수행할 수 있는 조회와 검증은 직접 한다. 저장소 소유자·이름·공개 범위 선택, 계정 인증, 플러그인 연결·권한 승인, 예약 작업 생성 승인처럼 사용자만 할 수 있는 결정이나 조작이 필요할 때만 요청한다.
- 사용자가 설치 방법을 다시 설명하게 하거나, 문서에 답이 있는 내용을 포괄적으로 되묻지 않는다.

## 설치 안전 규칙

- 설치 대상은 사용자가 이 템플릿에서 만든 파생 저장소여야 한다. 공개 템플릿 저장소에 사용자 데이터나 일일 리포트를 쓰지 않는다.
- 일반 읽기 전용 GitHub 앱을 쓰기 가능한 연결로 간주하지 않는다. 파일 생성·갱신과 기본 브랜치 커밋 도구가 확인되지 않으면 설치를 중단하고 필요한 권한만 설명한다.
- 저장소 접근 권한이나 계정 승인이 없는데 설치를 완료했다고 말하지 않는다.
- 설정 과정에서 날짜, 준비 상태, 출처 SHA, 멱등성 등 `README.md`와 `automation/codex-report-task.md`의 운영 안전 규칙을 약화하지 않는다.
- 자동 설치나 무인 설치라고 표현하지 않는다. 최초 저장소 생성, 권한 승인, Actions 활성화, 예약 작업 등록에는 사용자 조작이 필요할 수 있음을 분명히 한다.

## 완료 판정

- `SETUP.md`의 **설치 완료 확인** 항목을 기준으로 검증한다.
- 최초 Actions 실행, 당일 후보 파일, 06:20 `Asia/Seoul` 예약 작업, 예약 작업의 GitHub 쓰기 도구 사용 가능 여부를 확인한다.
- 가능한 경우 예약 작업을 한 번 수동 실행하여 Markdown과 HTML 리포트가 같은 커밋에 생성되고, 같은 입력의 재실행이 새 커밋을 만들지 않는지 확인한다.
- 검증하지 못한 항목은 미확인으로 보고하며, 필수 항목이 남아 있으면 설치 완료라고 말하지 않는다.
