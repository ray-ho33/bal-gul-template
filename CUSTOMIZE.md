# bal-gul 커스터마이즈 가이드

먼저 [SETUP.md](SETUP.md)의 설치 완료 체크리스트를 통과한 뒤 변경하세요. 이 가이드의 변경은 GitHub 웹에서 파일을 열고 연필 모양 **Edit this file** 버튼을 눌러 할 수 있습니다.

한 번에 한 종류만 바꾸고, 변경 설명을 적어 `main`에 커밋한 뒤 Actions를 수동 실행해 확인하는 것을 권장합니다.

## 바꿔도 되는 범위

- 1차 후보를 고르는 갈등·기관 키워드
- ChatGPT가 최종 O/X를 정하는 판별 기준
- 69개 신문사 항목의 1:1 교체

날짜 규칙, 후보 JSON 스키마, 준비 상태 검증, source/blob SHA 최신성 검사, 멱등성, 커밋 대상 제한은 바꾸지 마세요. 이 항목은 잘못되거나 오래된 입력으로 리포트가 만들어지는 것을 막는 안전장치입니다.

## 1. 갈등·기관 키워드 바꾸기

파일: [scraper/filtering.py](scraper/filtering.py)

파일 위쪽에 두 목록이 있습니다.

- `CONFLICT_KEYWORDS`: 갈등, 반발, 소송처럼 실제 마찰을 나타내는 단어
- `AGENCY_KEYWORDS`: 시청, 정부, 교육청처럼 공공기관을 나타내는 단어

기사 제목과 요약에 **두 목록의 단어가 각각 하나 이상** 있어야 1차 후보가 됩니다.

GitHub 웹에서 목록 안에 같은 형식의 따옴표 문자열을 추가하거나 제거하세요.

```python
CONFLICT_KEYWORDS = (
    "갈등",
    "반발",
    "내가 추가할 단어",
)
```

주의할 점:

- 너무 넓은 단어를 넣으면 후보가 급격히 많아집니다.
- 최종 판별은 별도 단계이므로 키워드만으로 양성 여부가 결정되지는 않습니다.
- 지명 뒤의 `시`, `군`, `도`를 기관 신호로 찾는 별도 규칙이 이미 있습니다. 정규식은 수정하지 않는 것을 권장합니다.
- Python 쉼표와 따옴표를 지워 문법을 깨뜨리지 않도록 합니다.

변경 후 Actions의 **Run workflow**를 한 번 실행해 `data/candidates-YYYY-MM-DD.json`이 정상 생성되는지 확인합니다.

## 2. 최종 판별 기준 바꾸기

파일: [automation/codex-report-task.md](automation/codex-report-task.md)

**3. 판별 규칙** 절만 수정하세요. 현재 기준은 다음 세 조건을 모두 요구합니다.

1. 현재 진행 중인 실제 갈등
2. 확인 가능한 민간 주체
3. 구체적으로 식별 가능한 정부·공공기관 상대

다른 주제로 바꿀 때는 다음을 함께 적어야 결과가 안정적입니다.

- 양성에 반드시 필요한 조건
- 명시적으로 제외할 사례
- 각 양성 항목에 기록할 필드
- 제목과 요약에 없는 사실을 추측하지 않는 규칙

예를 들어 노동 갈등만 보고 싶다면 양성 조건에 노동자·노조·사업자 등 허용할 민간 주체와 공공기관 상대 조건을 구체적으로 쓰고, 개발·환경 갈등을 제외 목록에 추가할 수 있습니다.

다음 절은 수정하지 마세요.

- 1절의 날짜와 readiness gate
- 2절의 신뢰하지 않는 입력 규칙
- 4절의 두 리포트·Provenance 계약
- 5절의 최신성·커밋·멱등 규칙
- 7절의 금지 사항

판별 기준을 바꾼 뒤에는 다음 날 자동 실행을 기다리기 전에 오늘 후보로 예약 작업을 한 번 수동 실행해 Markdown과 HTML의 의미가 같은지 확인하세요.

## 3. 신문사 목록 바꾸기

파일: [korean_regional_newspapers.json](korean_regional_newspapers.json)

현재 계약은 6개 권역, 총 69개 고유 신문사를 요구합니다. 각 항목에는 다음 값이 있습니다.

- `name`: 중복되지 않는 신문사 이름
- `homepage`: `http://` 또는 `https://` 홈페이지 주소
- `namu_wiki`: 목록의 참고 출처 주소

이번 배포판에서는 전체 수를 바꾸지 말고 기존 항목 하나를 새 항목 하나로 **1:1 교체**하세요. 다음 조건을 모두 지켜야 합니다.

- 전체 권역 수 6개 유지
- 전체 신문사 수 69개 유지
- 이름과 홈페이지 중복 금지
- 원래 JSON 구조와 필드 유지
- 유효한 HTTP 또는 HTTPS 주소 사용

`sites_total == 69`는 [scraper/schema.py](scraper/schema.py), 수집 workflow, 테스트, readiness 계약에 함께 고정되어 있습니다. 69를 다른 수로 바꾸는 일은 단순 커스터마이즈가 아니라 데이터 계약 변경이므로 이 가이드의 범위 밖입니다.

교체 후 Actions를 수동 실행하고 다음을 확인하세요.

- workflow가 후보 파일을 만들었는지
- `stats.sites_total`이 69인지
- 성공 수와 실패 목록 수의 합이 69인지
- 새 신문사 이름이 후보 또는 실패 목록에 나타나는지

## 4. 변경 확인과 되돌리기

1. 저장소의 **Actions**에서 **Collect article candidates**를 수동 실행합니다.
2. 성공하면 오늘 후보 JSON과 실행 로그를 확인합니다.
3. 판별 기준을 바꿨다면 ChatGPT Scheduled에서 리포트 작업을 한 번 수동 실행합니다.
4. 두 리포트와 Provenance를 확인합니다.

문제가 생기면 GitHub의 해당 커밋 화면에서 **Revert**를 사용하거나, 파일의 **History**에서 정상 버전을 열어 내용을 되돌리세요. 여러 설정을 한 커밋에서 함께 바꾸지 않으면 원인을 찾고 복구하기 쉽습니다.

설치 자체가 아직 완료되지 않았다면 커스터마이즈보다 [SETUP.md](SETUP.md)의 첫 수동 실행을 먼저 해결하세요.
